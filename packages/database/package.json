{
  "name": "@luogu-discussion-archive/db",
  "version": "0.0.0",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "dev": "tsc --watch",
    "build": "tsc",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev --skip-generate",
    "db:deploy": "prisma migrate deploy"
  },
  "dependencies": {
    "@prisma/client": "^6.9.0"
  },
  "devDependencies": {
    "@luogu-discussion-archive/typescript-config": "workspace:*",
    "@types/node": "^20.19.0",
    "prisma": "^6.9.0",
    "typescript": "^5.8.3"
  }
}
